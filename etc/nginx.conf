events {}

http {
	include /etc/nginx/mime.types;

	server {
		listen 80;
		server_name _;
		#root /home/box/web;
		location ^~ /uploads/ {
			root /home/box/web;
		}

		location ~* ^.+\.\w+$ {
			root /home/box/web/public;
		}

		location / {
			return 404;
		}
	}

}